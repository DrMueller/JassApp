@using MudBlazor
@using Toolbelt.Blazor.SpeechSynthesis
@if (!IsLoading)
{
    <MudSelect ValueChanged="HandleVoiceChangedAsync"
               Value="SelectedVoice"
               T="SpeechSynthesisVoice"
               Label="Stimme">
        @foreach (var voice in Voices!)
        {
            <MudSelectItem Value="@voice">@voice.Name - @voice.Lang</MudSelectItem>
        }
    </MudSelect>
}